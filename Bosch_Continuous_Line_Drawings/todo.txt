.draw points to resembling the picture
    .compute the mean grayscale for each k square
    .map that value to k by k
    .generate that many random points in the k square


.study OR tools TSP enough to use it on 2D coordinates

.Run TSP concorde with qsopt on Windows
.Compile GLPK or SCIP to wasm, which ever one works best
.Use it in wasm
.Compile concorde to wasm
.wrap GLPK or SCIP with qsopt headers
.Compile together with concorde in wasm
.Solve TSP in Javascript
